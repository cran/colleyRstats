% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latex-output.R
\name{emit_overleaf}
\alias{emit_overleaf}
\title{Bundle an analysis into an Overleaf-ready folder}
\usage{
emit_overleaf(
  x,
  dir,
  figures = TRUE,
  methods = c("ggstatsplot", "effectsize"),
  title = "Results",
  plain = NULL,
  columns = 1,
  overwrite = FALSE
)
}
\arguments{
\item{x}{What to emit. Accepts a \code{\link[=report_all]{report_all()}} result (one section per
dependent variable, with figures), an \code{\link[=analyze_and_report]{analyze_and_report()}} result, a
named list of sentence vectors, or a single character vector.}

\item{dir}{Output directory (created if needed).}

\item{figures}{Whether to save figures for sections that carry a plot.
Default \code{TRUE}.}

\item{methods}{Methods to cite (passed to \code{\link[=cite_methods]{cite_methods()}}) for
\code{references.bib}; \code{NULL} to skip the bibliography.}

\item{title}{Title used in the generated \code{main.tex}. Default
\code{"Results"}.}

\item{plain}{Whether to expand the colleyRstats macros to plain LaTeX (so no
\code{.sty} / \code{\\usepackage} is needed). Default \code{NULL} follows
\code{getOption("colleyRstats.macros")} (i.e. plain when that is
\code{FALSE}).}

\item{columns}{Figure width preset passed to \code{\link[=save_paper_figure]{save_paper_figure()}}.}

\item{overwrite}{Overwrite existing files in \code{dir}? Default \code{FALSE}.}
}
\value{
Invisibly, a list with the paths written (\code{dir}, \code{main},
\code{results}, \code{sections}, \code{figures}, \code{bib}, \code{sty}).
}
\description{
Writes everything a manuscript needs into one directory you can drag into
Overleaf and compile immediately: a \code{main.tex} that already
\code{\\input}s the results, one \code{.tex} per result section, the figures,
a \code{references.bib}, and -- unless macros are expanded inline --
\code{colleyRstats.sty}. This is the one-call end of the "R analysis to
compiled PDF" pipeline.
}
\examples{
\donttest{
out <- report_all(mtcars, dvs = c("mpg", "disp"), iv = "cyl")
emit_overleaf(out, dir = file.path(tempdir(), "paper"), overwrite = TRUE)
}
}
