% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{analyze_and_report}
\alias{analyze_and_report}
\title{Analyze one dependent variable and produce everything a paper needs}
\usage{
analyze_and_report(
  data,
  dv,
  iv,
  design = c("between", "within"),
  ylab = dv,
  xlabels = NULL,
  plotType = "boxviolin",
  sink_to = NULL
)
}
\arguments{
\item{data}{the data frame}

\item{dv}{the dependent variable (column name as string)}

\item{iv}{the independent variable (column name as string); coerced to a
factor if it is not one already}

\item{design}{\code{"between"} for between-subjects data (default) or
\code{"within"} for repeated measures}

\item{ylab}{label for the dependent variable; defaults to \code{dv}}

\item{xlabels}{optional labels for the x-axis}

\item{plotType}{either "box", "violin", or "boxviolin" (default)}

\item{sink_to}{optional path of a \code{.tex} file; the methods sentence,
omnibus result, and post-hoc sentences are written there so a manuscript
can \code{\\input{}} them}
}
\value{
Invisibly returns a list with components \code{plot} (the ggplot),
\code{methods} (assumption-check sentence), \code{text} (omnibus result),
\code{posthoc} (post-hoc sentences, or \code{NULL} for two groups), and
\code{sentences} (all text combined, in manuscript order).
}
\description{
One-call pipeline for a single dependent variable: checks the assumptions
(producing a ready-made methods sentence via \code{\link[=assumption_methods_text]{assumption_methods_text()}}),
builds the matching \pkg{ggstatsplot} figure with automatic
parametric/non-parametric selection, reports the omnibus test via
\code{\link[=reportggstatsplot]{reportggstatsplot()}}, and -- for more than two groups -- reports the
significant post-hoc comparisons via \code{\link[=reportggstatsplotPostHoc]{reportggstatsplotPostHoc()}}.
}
\examples{
\donttest{
result <- analyze_and_report(mtcars, dv = "mpg", iv = "cyl")
result$plot
}
}
