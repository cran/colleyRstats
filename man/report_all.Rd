% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{report_all}
\alias{report_all}
\title{Analyze and report several dependent variables at once}
\usage{
report_all(
  data,
  dvs,
  iv,
  design = c("between", "within"),
  labels = NULL,
  xlabels = NULL,
  plotType = "boxviolin",
  sink_dir = NULL
)
}
\arguments{
\item{data}{the data frame}

\item{dvs}{character vector of dependent variable column names}

\item{iv}{the independent variable (column name as string)}

\item{design}{\code{"between"} (default) or \code{"within"}}

\item{labels}{optional named character vector mapping a dv name to its
axis label, e.g. \code{c(tlx_mental = "Mental Demand")}}

\item{xlabels}{optional labels for the x-axis, passed to every plot}

\item{plotType}{either "box", "violin", or "boxviolin" (default)}

\item{sink_dir}{optional directory; each dv's sentences are written to
\code{<sink_dir>/<dv>.tex} so a manuscript can \code{\\input{}} them}
}
\value{
Invisibly returns a list with components \code{results} (named list
of \code{\link[=analyze_and_report]{analyze_and_report()}} results), \code{summary} (data frame with one
row per dv: method, statistic, p.value, and Holm-adjusted \code{p.holm}),
and \code{combined_plot} (a patchwork figure, or \code{NULL} when
patchwork is not installed).
}
\description{
Runs \code{\link[=analyze_and_report]{analyze_and_report()}} for each dependent variable (e.g., all
questionnaire scales of a study) and additionally returns a summary table
of the omnibus tests with Holm-adjusted p-values across the dependent
variables, plus -- when \pkg{patchwork} is installed -- a combined figure.
}
\examples{
\donttest{
out <- report_all(mtcars, dvs = c("mpg", "disp"), iv = "cyl")
out$summary
}
}
